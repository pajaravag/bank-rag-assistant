# Scraping
httpx>=0.27
beautifulsoup4>=4.12
lxml>=5.0

# Config
pydantic>=2.7
pydantic-settings>=2.3

# Vector DB + embeddings
qdrant-client>=1.9
sentence-transformers>=3.0

# LLM
groq>=0.9

# API + UI
fastapi>=0.111
uvicorn[standard]>=0.30
streamlit>=1.36

# Observability (Phoenix via OpenTelemetry)
opentelemetry-api>=1.25
opentelemetry-sdk>=1.25
opentelemetry-exporter-otlp-proto-http>=1.25

# Tests (kept in the image so evaluators can run them in-container)
pytest>=8.0
